name: Health Check

on:
  schedule:
    - cron: '0 8 * * *' # Todos los días a las 8:00 UTC

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Check deployed app health
        uses: Jtalk/url-health-check-action@v4
        with:
          url: https://full-stack-open-pokedex-tbqb.onrender.com
          max-attempts: 3
          retry-delay: 10s
          follow-redirect: true
